package lecture;
//student 오버로딩 후 실행
public class StudentTest {

	public static void main(String[] args) {
		Student student = new Student(); // 인스턴스(객체를 이루는 것) 변수
		//생성자가 없을 경우 기본 생성자(디폴트)를 만들어 준다(현재 나는 사람이다 출력)
		//부모 위로 올라가게 되어서 모든 값을 가져온다
	
	}
}

