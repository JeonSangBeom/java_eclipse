package lecture;

public class Sample {
	//객체 멤버 필드(인스턴스 변수)
	public int a;
	private int b;
	public static int d = 10; //static을 사용 하면 객체 멤버가 아닌 클래스로 접근을 하여야 된다(static영역에 들어감)
	//static를 붙이는 이유는 공유를 목적으로 함에 있다(클래스 변수)
	int c; // 안쓰면 디폴트
}
